# Contributing

Thanks for contributing to Pi Native Subagents.

## Development

Requires Node.js 22.19.0 or newer.

```bash
npm install
npm run check
```

Keep changes focused, explain the motivation in the pull request, and run `npm run check` before submitting. This project is experimental, so avoid claiming compatibility that has not been tested.
